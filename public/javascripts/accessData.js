// $(document).ready(function() {
  // for(var i in all_hotels) {
  //   var option = "<option value='" + all_hotels[i]._id.toString() + "'>" + all_hotels[i].name + "</option>";
  //   $('#hotel-dropdown').append(option);
  // }
//   for(var i in all_restaurants) {
//     var option = "<option value='" + all_restaurants[i]._id.toString() + "'>" + all_restaurants[i].name + "</option>";
//     $('#restaurant-dropdown').append(option);    
//   }
//   for(var i in all_things_to_do) {
//     var option = "<option value='" + all_things_to_do[i]._id.toString() + "'>" + all_things_to_do[i].name + "</option>";
//     $('#thingsToDo-dropdown').append(option);
//   }
// })